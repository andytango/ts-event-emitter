{
  "name": "event-emitter-lite",
  "version": "1.0.0",
  "description": "Minimal event emitter with typescript",
  "main": "dist/index.js",
  "author": "andytango",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "touch ./dist/tmp && rimraf ./dist/*",
    "compile": "tsc",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.1",
    "jest-extended": "^0.11.5",
    "prettier": "^2.4.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
